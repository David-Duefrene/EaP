import React from 'react'

import CSS from './Notification.module.css'

const Notification = () => {
	return (
		<button className={CSS.Notification} type='button'>!</button>
	)
}

export default Notification
